{{wsd-capture-view
    viewId=model.viewId
    captureInfo=model.capture
    drilldownInfoParam=model.queryParams.drilldownInfoParam
    timelines=metricTimelines
    toggleMetricTimeline=(route-action "toggleMetricTimeline")
    select=(route-action "select")
    drillDown=(route-action "drillDown")
}}
