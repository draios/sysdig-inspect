{{#if height width}}
    <g>
        {{#each columns as |column|}}
            <rect
                class="sd-time-series__column"
                x={{column.x}}
                y={{column.y}}
                width={{column.w}}
                height={{column.h}}
            ></rect>
        {{/each}}
    </g>
{{/if}}