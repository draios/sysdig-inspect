{{#if viewStore.isLoading}}
    Loading...
{{/if}}

{{#if viewStore.hasLoaded}}
    {{#sd-list items=views key="viewId" select=(action "selectView") as |view|}}
        {{view.name}}
    {{/sd-list}}
{{/if}}
